# Production Folder

This folder contains production-side demo files used to illustrate merge scenarios.

- `shared.txt`: production-version of a shared file (different from development)

- `conflict_demo.txt`: a file with a conflicting line example
